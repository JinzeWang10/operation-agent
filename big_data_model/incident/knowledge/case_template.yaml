# ============================================================
# 复盘记录模板 —— 填完即入案例台账,零转换
# 用法: 复制本文件,按注释填写,交给 load_case_yaml() 校验入库
# 根因粒度标准: 类别 × 定位对象,够缩小排查范围即可,
#               不要求定位到代码行/配置项
# ============================================================

事件ID: INM-20260611-0000000        # 事件单号原样填写
系统: 某某系统                       # 受影响系统(用事件单上的系统名)
发生时间: 2026-06-11T10:32:00       # 异常开始时间,ISO 格式
来源: 人工复盘                       # 固定值,不要改

# ---- 现场(尽量写当时看到了什么,包括"什么是正常的" ----
现场摘要: >-
  (示例)BPC 响应率 99.5% 降至 92%,日志出现 connection timeout,
  主机与 Redis 指标正常。

# ---- 真实根因(命中率统计的唯一口径) ----
回填:
  类别: DB                          # 根因类别,与 pattern ID 前缀同一套分类法
  定位对象: DB1                      # 系统/组件/实例
  描述: 核保规则发布引入新 SQL 全表扫描   # 一句话,可写触发因素
回填时间: "2026-06-12T09:40:00"

# ---- 以下字段人工复盘通常留空(Agent 运行后自动填) ----
假设轨迹: ""
Top3: []
未命中已知模式: false
原始: {}
