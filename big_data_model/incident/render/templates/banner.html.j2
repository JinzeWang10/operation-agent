{# 疑似根因 banner — vm = BannerVM #}
<section class="banner">
  <div class="banner-head">
    <div class="banner-title">{{ vm.title }}</div>
    <div class="banner-note">{{ vm.note }}</div>
  </div>
  <ul class="banner-rules">
    {% for r in vm.rules %}<li>{{ r }}</li>{% endfor %}
  </ul>
</section>
